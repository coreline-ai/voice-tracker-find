# thinktank 설치 안내 (받는 사람용)

폰으로 녹음하면 PC 가 전사·정리해서 Obsidian 노트로 만들어 주는 개인용 시스템입니다.
**데이터가 당신의 기기 밖으로 나가지 않습니다**(클라우드 없음).

## 먼저 필요한 것 (없으면 동작하지 않음)
- **Windows 10/11 PC**
- **NVIDIA GPU** — 전사(STT) 속도의 핵심. 없으면 매우 느립니다.
- **Claude Code 구독** — 분류·정리에 당신의 Claude 계정을 씁니다(추가 API 비용 없음).
  아직 없으면 먼저 설치·로그인: https://claude.com/claude-code
- **안드로이드 폰** (녹음기 앱)

## 설치 — 3단계

**① 압축을 풉니다.** 원하는 폴더에 이 zip 을 풉니다.

**② Claude Code 를 그 폴더에서 열고, 이렇게 말합니다:**
```
SETUP.md 대로 설치해줘
```
그러면 Claude 가 알아서 진행합니다 — ffmpeg·Python 환경·로컬 임베딩(Ollama)·자동 실행 등록까지.
중간에 **로그인·방화벽(관리자)·폰 연결** 같은 건 당신에게 물어봅니다. 시키는 대로 하면 됩니다.

**③ 폰에 앱을 설치하고 연결합니다.**
같은 폴더의 `thinktank-recorder.apk` 를 폰에 설치 → 앱 설정 탭에서 **서버 주소·토큰**을
입력(Claude 가 값을 알려줍니다) → "연결 확인" → 녹음 시작.

## 그 다음
낮에는 폰에 쌓였다가 자동으로 PC 로 넘어가고, 처리되면 폰에 **"새 노트" 알림**이 옵니다.
노트는 폰 앱에서 폴더별로 보거나, PC 의 Obsidian 볼트(`~/thinktank-vault`)에서 봅니다.

문제가 생기면 Claude Code 에게 "설치가 안 돼, SETUP.md 8번(첫 실행 검증)부터 다시 봐줘"
처럼 말하면 됩니다. 자세한 설치 단계는 `SETUP.md` 에 있습니다.
