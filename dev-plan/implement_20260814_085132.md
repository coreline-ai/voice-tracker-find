# implement_20260814_085132.md

작성 일시: `2026-08-14 08:51:32 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
AI R Voice 남은 출고 전 검증 항목을 삼성폰 실기기·장시간 처리·서버 연동·배포 게이트까지 완료한다.

## 개발 범위
승인된 Samsung SM-S931N에서 Android QA와 데이터 유지성 검증, 30분·1시간·2시간 STT→Gemma 장시간 검증, LAN Receiver·웹 대시보드 결과 대조, OAuth·릴리스·문서·저장소 최종 정리

## 제외 범위
- 새 STT 또는 요약 LLM 교체
- GCP/외부 클라우드 staging 배포
- 사용자 승인 없는 GitHub 저장소 이름 변경

## 참조 문서
- 이전 개발 계획: dev-plan/implement_20260813_205318.md
- README.md
- HANDOFF.md
- dev-plan/implement_20260813_205318.md
- dev-plan/implement_20260731_193413.md
- docs/qa/ai-r-voice-rebrand-implementation-20260813.md
- docs/qa/oauth-llm-e2e-runbook.md
- android-app/scripts/install_samsung_preview.sh
- android-app/scripts/run_device_qa.sh

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 기능은 유지보수 가능한 최소 책임 단위로 나누고, 각 단위의 역할과 경계를 계획에 적는다.
- 계획 밖 옵션/확장/부가 기능을 만들기 위한 분리는 하지 않는다.
- 공통화는 계획 범위 안의 중복을 줄일 때만 수행하고 과도한 추상화는 피한다.
- 기존 프로젝트 API, 공식 SDK, 표준 라이브러리를 우선 검토하고 새 의존성은 범위상 필요할 때만 포함한다.
- 승인 단말은 `R3CY40PXCAP / Samsung SM-S931N`으로 고정한다. 다른 ADB 단말에는 설치·테스트·삭제 명령을 실행하지 않는다.
- 지속 데이터 검증에서는 `com.coreline.ai.voice.qa`를 uninstall/clear하지 않고 동일 서명 APK의 `adb install -r`만 사용한다.
- STT는 SenseVoice/기존 파일 STT 경로, 요약은 기본 Gemma 3 1B 경로를 유지한다. 모델 교체는 이번 범위가 아니다.
- 서버는 기존 LAN Receiver와 Web Console을 사용한다. 외부 클라우드/GCP 배포는 이번 계획에서 실행하지 않는다.
- 실측 결과가 없으면 완료로 표시하지 않고 `NOT_EXECUTED` 또는 `BLOCKED`와 사유를 기록한다.

## Phase 상태 요약
- [ ] Phase 1. 삼성폰 QA 및 설치 보호 완료
- [ ] Phase 2. 앱 데이터·Room 마이그레이션 유지성 완료
- [ ] Phase 3. 장시간 STT·Gemma 실측 완료
- [ ] Phase 4. Receiver·웹 대시보드 E2E 완료
- [ ] Phase 5. OAuth·릴리스 준비 완료
- [ ] Phase 6. 문서·저장소·최종 검증 완료

## QA 관점
- [ ] 설치 대상이 승인 단말과 일치하지 않을 때 명령이 사전에 중단된다.
- [ ] 업데이트 전후 녹음, 원문 STT, 요약, 설정, 모델 파일, Room row가 감소하지 않는다.
- [ ] 30분·1시간·2시간 입력의 전체 음성 구간이 STT 결과에 포함되고, 누락·중복·순서 역전이 없다.
- [ ] STT/Gemma 진행 중 앱 종료·프로세스 중단·네트워크 단절 뒤 checkpoint와 재시도가 원본을 훼손하지 않는다.
- [ ] 업로드 receipt, 서버 파일, Markdown 노트, 앱 노트가 동일 recording ID와 파일 hash를 가진다.
- [ ] dashboard 인증 실패는 401, 정상 token은 200이며 token·원문·로컬 경로가 로그와 응답에 노출되지 않는다.
- [ ] OAuth가 보류된 경우 로컬 Gemma 기본 경로가 계속 동작하고, 임의의 외부 전송이 발생하지 않는다.
- [ ] unsigned release와 실계정 미검증을 배포 완료로 오표기하지 않는다.

## 현재 기준선

- [x] `main`과 `origin/main`이 `d74bf4d docs: add bilingual repository introduction`에서 일치한다.
- [x] Python/Web 자동 검증과 Android unit/lint/assemble 검증은 기존 보고서 기준 통과 상태다.
- [x] 현재 제품 identity는 AI R Voice, QA package는 `com.coreline.ai.voice.qa`다.
- [x] LAN Receiver와 Web Console의 구현 및 기본 인증 계약은 존재한다.
- [x] 현재 Samsung 단말에서 최신 QA APK를 설치하고 preflight·cold start·core connected QA를 다시 실행했다.
- [x] 현재 QA package 기준 Room migration과 데이터 삭제 없는 update-install 경로를 재검증했다. 사용자 recording fixture 기반의 손실 비교는 남아 있다.
- [x] 격리 deviceTest package에서 공식 SenseVoice 모델 Wi-Fi 다운로드·설치·파일 STT를 통과시켰다.
- [ ] 실제 30분·1시간·2시간 음원으로 장시간 STT→Gemma를 측정한다.
- [ ] 실계정 OAuth와 release signing은 외부 승인·credential 제공 전까지 보류한다.

---

## Phase 1. 삼성폰 QA 및 설치 보호

### 목표
- 승인된 Samsung SM-S931N 한 대에 최신 QA preview를 안전하게 설치하고, 녹음 앱의 핵심 경로를 실기기에서 확인한다.

### 구현 태스크
- [x] ADB preflight에서 serial, manufacturer, model, Android API, 배터리, 저장공간을 증적 파일에 기록한다.
- [x] `android-app/scripts/install_samsung_preview.sh`로 `com.coreline.ai.voice.qa`만 빌드·설치한다.
- [x] 최초 설치가 필요한 경우에만 사용자 승인 플래그 `--allow-first-install`을 사용한다.
- [x] 기존 `.qa` 설치가 있으면 `adb install -r`만 허용하고 uninstall·clear·reset 명령을 사용하지 않는다.
- [x] launcher 표시명 `AI R Voice`, 아이콘, version, application ID, MainActivity를 확인한다.
- [ ] 앱 cold start, 녹음 시작·중지, `.part`에서 최종 `.m4a` 확정, 앱 전환·화면 잠금 후 foreground recording을 확인한다.
- [ ] 녹음 목록에서 재생·삭제·상세 진입이 정상인지 확인하고, 삭제는 테스트 fixture에만 수행한다.
- [x] QA package에 귀속된 logcat에서 FATAL EXCEPTION, ANR, OOM이 없는지 확인한다.

### 자체 테스트
- [x] `run_device_qa.sh --case preflight`가 승인 Samsung 외 단말 명령을 거부하는지 확인한다.
- [x] `run_device_qa.sh --case core`를 실행하고 connected instrumentation failure/error가 0인지 확인했다. 모델 의존 테스트는 명시적으로 skip되었다.
- [x] RecorderService connected test에서 녹음 session/chunk 확정과 `.part` 제거를 확인했다.
- [x] `.qa` package path와 앱 data path가 테스트 전후 유지되고, 다른 package가 설치·삭제되지 않았는지 확인했다.

### 이슈 및 수정
- [x] `run_sensevoice_wifi_download_qa.sh`가 Java 17로 실행되어 LiteRT-LM class version 65를 읽지 못하던 문제를 Java 21 resolver 적용으로 수정했다.
- [x] QA logcat이 다른 앱의 오래된 crash를 수집하던 문제를 AI R Voice package 필터로 수정했다.
- [x] 스크립트가 비승인 단말에 접근하거나 persistent `.qa` 데이터를 초기화하지 않는 것을 확인했다.
- [ ] `.qa` 서명 불일치, Gemma/SenseVoice 모델 미설치, Android 16 foreground service, 실제 UI playback은 별도 gate로 남아 있다.

### 완료 조건
- [x] 승인 Samsung에서 최신 QA APK 설치·cold start 통과
- [ ] 녹음·재생·앱 전환·화면 잠금 smoke 통과
- [ ] fatal/ANR/OOM 0건 및 비파괴 설치 증적 저장
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능


## Phase 2. 앱 데이터·Room 마이그레이션 유지성

### 목표
- 앱 업데이트와 Room schema migration 뒤에도 사용자가 만든 녹음과 처리 결과가 사라지지 않는지 검증한다.

### 구현 태스크
- [ ] 테스트 fixture로 녹음, 원문 STT, Gemma 요약, 서버 설정, 모델 marker를 `.qa`에 준비한다.
- [ ] 업데이트 전후 recording count, source file path/size/hash, transcript, summary, settings, model size를 비교하는 read-only 검사 절차를 정리한다.
- [x] Room schema migration instrumentation(3→4부터 9→10까지 7개 테스트)을 삼성폰에서 재실행했다.
- [ ] 기존 앱을 자동 삭제하지 않는 정책과 새 application ID가 신규 sandbox를 사용하는 정책을 문서와 실제 동작에서 일치시킨다.
- [ ] 업데이트 후 노트 동기화와 기존 원문전사보기 진입이 정상인지 확인한다.
- [ ] 모델 재다운로드가 불필요하고 명시적 삭제 전에는 모델 파일이 유지되는지 확인한다.

### 자체 테스트
- [x] Room migration 7개 테스트 성공, schema/table/column 누락 0을 확인했다. 현재 fixture는 schema 3→10 범위다.
- [ ] `adb install -r` 전후 데이터 개수 감소 0, hash 불일치 0을 확인한다.
- [ ] 앱 재시작·강제 종료 후에도 녹음·STT·요약 결과와 설정이 유지되는지 확인한다.
- [ ] 중단된 STT/Gemma job이 checkpoint를 유지하고 재개 후 중복 row를 만들지 않는지 확인한다.

### 이슈 및 수정
- [ ] Room migration 실패, 서명 불일치, 모델 path 변경, DataStore key 변경, 기존 summary 유실을 발견하면 해당 원인과 수정 결과를 기록한다.
- [ ] 기존 `.qa`의 서명 키가 다르면 데이터를 삭제하지 않고 재현 가능한 별도 설치/키 정합화 해결책을 기록한다.

### 완료 조건
- [ ] Room 1→10 migration 통과
- [ ] update-install 전후 데이터·모델·설정 손실 0
- [ ] 강제 종료·재시작·재개 테스트 통과
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능


## Phase 3. 장시간 STT·Gemma 실측

### 목표
- 실제 긴 녹음이 끝까지 STT되고, 같은 STT 텍스트를 기준으로 Gemma 3 1B 요약 1건이 안정적으로 생성되는지 측정한다.

### 구현 태스크
- [x] 승인 삼성폰과 격리 deviceTest package에서 SenseVoice 공식 artifact를 SHA-256 검증 후 설치하고 파일 STT를 실행하는 경로를 수정·통과시켰다.
- [ ] 30분·1시간·2시간 fixture 또는 실음원을 준비하고 각 파일의 원본 duration·size·SHA-256을 기록한다.
- [ ] 처리 시작 전 원본 snapshot과 job ID를 만들고, STT segment·checkpoint·Gemma node 상태를 단계별로 기록한다.
- [ ] SenseVoice/STT 결과가 파일 끝까지 포함되는지 segment timestamp와 원본 duration으로 검증한다.
- [ ] Gemma 3 1B 기본 모델로 동일 STT 텍스트에 대해 요약을 실행하고 모델명·입력 문자 수·출력 문자 수를 기록한다.
- [ ] 앱 백그라운드 전환, 화면 잠금, 프로세스 종료, worker 중단, pause/resume/cancel을 각 1회 검증한다.
- [ ] elapsed time, peak PSS, CPU/thermal, battery, working storage를 테스트별로 기록한다.
- [ ] 2시간 초과 입력은 실행하지 않고 사용자에게 설명 가능한 제한 오류로 종료되는지 확인한다.
- [ ] 장시간 실행 중 원본 오디오와 기존 완료 결과를 덮어쓰지 않도록 처리한다.

### 자체 테스트
- [ ] 30분·1시간·2시간 각각 STT coverage 100%, timestamp 누락 0, 중복 segment 0을 확인한다.
- [ ] 각 결과에 요약 1건, 동일 recording/session ID, Gemma 3 1B provenance가 저장되는지 확인한다.
- [ ] 중단 후 재개 결과의 transcript/tree hash가 중단 전 정상 기대값과 일치하는지 확인한다.
- [ ] 실패 시 `FAILED_RECOVERABLE` 등 상태와 재시도 원인이 앱·DB·로그에서 일치하는지 확인한다.
- [ ] 장시간 테스트 중 외부 네트워크 요청이 발생하지 않는지 확인한다.

### 측정 결과 기록 형식

| 길이 | 원본 duration/hash | STT coverage | 요약 상태 | elapsed | peak PSS | thermal/battery | 재개 결과 |
|---|---|---:|---|---:|---:|---|---|
| 30분 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 |
| 1시간 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 |
| 2시간 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 | 기록 예정 |

### 이슈 및 수정
- [x] 공식 SenseVoice 격리 QA에서 Java runtime 불일치 원인을 재현하고 수정했다.
- [ ] 45초 또는 5분 음원에서 재현됐던 끝부분 누락, 빈 결과, 요약 미생성, worker 무한 대기 원인을 실제 증적과 함께 분류한다.
- [ ] checkpoint·파일 경로·시간 제한·메모리 압박 문제를 발견하면 코드 수정 후 동일 길이 테스트를 재실행한다.
- [ ] 성능이 부족해도 이번 범위에서 모델 교체를 임의로 진행하지 않고 측정값과 후속 결정으로 남긴다.

### 완료 조건
- [ ] 세 길이의 STT 끝부분 누락 0
- [ ] 세 길이의 Gemma 요약 결과와 provenance 확인
- [ ] 중단·재개·취소 동작과 원본 보호 확인
- [ ] 측정표·로그·DB 상태 증적 저장
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능


## Phase 4. Receiver·웹 대시보드 E2E

### 목표
- 앱 녹음 1건이 LAN Receiver, 서버 저장소, Markdown note, Web Console, 앱 노트에 동일하게 반영되는지 확인한다.

### 구현 태스크
- [x] 임시 Receiver에서 `/health`, dashboard 정적 페이지, dashboard summary 인증·응답 계약을 확인했다.
- [ ] 삼성 앱에서 테스트 녹음 1건을 업로드하고 upload receipt의 recording ID, chunk ID, filename, size, SHA-256을 기록한다.
- [x] 임시 Receiver에서 M4A 원본 보존과 upload receipt를 확인했다. 실제 STT/Markdown 자동 처리는 `RECEIVER_AUTO_PROCESS=0` 계약 테스트 범위로 분리했다.
- [x] 임시 Receiver에서 Markdown note 생성·조회와 M4A dashboard audio endpoint의 실제 바이트 일치를 확인했다. 브라우저 UI의 실제 재생 클릭은 남아 있다.
- [ ] 앱 노트 동기화에서 동일 note ID/title/content가 표시되고, 수동 동기화 무한 spinner가 발생하지 않는지 확인한다.
- [ ] Wi-Fi 동일 AP 환경과 서버 중지·재시작·일시적 네트워크 단절을 분리해 확인한다.
- [ ] 동기화 재시도는 동일 idempotency key를 사용하고 중복 파일·중복 note를 만들지 않도록 검증한다.

### 자체 테스트
- [x] 무자격 dashboard summary 요청 401, 올바른 임시 token 요청 200을 확인했다.
- [x] Receiver upload receipt·서버 M4A·dashboard audio의 파일 hash/바이트 일치를 확인했다.
- [ ] 삼성 앱·대시보드 UI에서 MD 내용 보기, 원문 STT 보기, M4A 재생을 실제 클릭으로 확인한다.
- [ ] 서버 재시작 후에도 업로드 대기 파일과 완료 파일의 상태가 복구된다.
- [ ] token, 원문 전체, PC 절대경로가 브라우저 console·HTTP 오류·서버 로그에 노출되지 않는다.

### 이슈 및 수정
- [ ] 서버 연결 실패, 401/403, 업로드 대기 고착, 노트 미생성, 앱 기록 사라짐, 대시보드 빈 상태를 재현 단계와 함께 기록한다.
- [ ] 서버 데이터와 앱 표시가 다르면 앱 캐시·동기화 API·파일 생성·DB 상태를 순서대로 대조하고 임의 삭제로 해결하지 않는다.
- [x] 삼성폰 `192.168.0.75` → Mac `192.168.0.2:18766` 실제 Wi-Fi 연결 확인은 20초 후 실패했다. Mac Receiver는 `0.0.0.0:18766`에서 기동되고 로컬 health는 200이므로 Mac 방화벽/AP isolation/network policy 확인이 선행되어야 한다. USB 우회는 사용하지 않았다.

### 완료 조건
- [ ] 업로드 receipt·원본 M4A·Markdown·앱 노트·대시보드 표시가 1건으로 일치
- [ ] MD 보기와 M4A 재생 성공
- [ ] 서버 재시작·일시 단절·재시도 통과
- [ ] 인증·민감정보 노출 검증 통과
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능


## Phase 5. OAuth·릴리스 준비

### 목표
- 이미 구현된 OAuth-first/local-Gemma fallback 계약과 release artifact를 실제 배포 조건에 맞춰 검토하되, 승인 없는 외부 계정·서명 작업은 완료로 처리하지 않는다.

### 구현 태스크
- [ ] `docs/qa/oauth-llm-e2e-runbook.md`에 따라 Anthropic·Codex·xAI별 QA 계정 E2E 실행 여부를 기록한다.
- [ ] 성공 시 provider/model/request ID/latency/usage만 증적에 남기고 token·authorization code·raw body는 저장하지 않는다.
- [ ] Network·429·5xx·timeout은 Gemma 1회 fallback, cancel·invalid request는 자동 fallback 없음인지 확인한다.
- [ ] OAuth 계정이 없으면 기본 로컬 Gemma 경로와 외부 전송 차단 상태를 release 전제 조건으로 확인한다.
- [ ] release signing credential 제공 여부를 확인하고, 미제공 시 unsigned APK를 명확히 `NOT_RELEASED`로 표시한다.
- [ ] 서명 APK/AAB가 준비되면 신규 설치·업데이트 설치·application ID·version·서명 검사를 실행한다.
- [ ] 개인정보·네트워크 전송·OAuth client ID·MIT/third-party notice 문서를 배포 패키지와 대조한다.

### 자체 테스트
- [ ] 실제 OAuth E2E는 Provider별 필수 행이 모두 실행된 경우에만 `executed=true`로 기록한다.
- [ ] OAuth 미실행 상태에서도 local Gemma summary가 정상 생성된다.
- [ ] signed artifact가 없으면 스토어 배포 완료 표시가 생성되지 않는다.
- [ ] APK/AAB 민감정보·private key·secret·refresh token 검사 결과 0을 확인한다.

### 이슈 및 수정
- [ ] 계정·client registration·signing credential이 없으면 코드를 임의의 값으로 채우지 않고 `DEFERRED_BY_OWNER`로 남긴다.
- [ ] provider 결과 provenance 불일치나 잘못된 fallback이 발견되면 OAuth adapter/router 범위에서만 수정한다.

### 완료 조건
- [ ] OAuth 실행 또는 보류 사유가 문서화됨
- [ ] local Gemma 기본 경로 검증 완료
- [ ] release 서명 상태와 artifact checksum 기록
- [ ] 개인정보·라이선스·secret 검사 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능


## Phase 6. 문서·저장소·최종 검증

### 목표
- 실제 결과와 문서를 일치시키고, clean checkout에서 재현 가능한 최종 handoff 상태를 만든다.

### 구현 태스크
- [ ] 각 Phase 증적 경로, 실행일, 단말, build variant, 결과를 이 계획과 QA 보고서에 반영한다.
- [ ] README의 “현재 상태”와 남은 항목을 실측 결과 기준으로 갱신한다.
- [ ] 최신 Android 화면 캡처와 Web Console 캡처가 현재 AI R Voice UI와 일치하는지 검토하고 필요한 경우 교체한다.
- [x] `scripts/verify_rebrand.py`가 무시된 `__pycache__`를 활성 legacy source로 오인하지 않도록 검증 범위를 수정하고 회귀 테스트를 추가했다.
- [x] Python pytest/ruff와 Android unit/lint/variant build/quality gate를 재실행했다.
- [ ] Git status에서 의도하지 않은 추적 변경이 없는지 확인하고, 사용자의 로컬 `korean_voice_sample.mp3`는 임의로 삭제·추적하지 않는다.
- [ ] GitHub 저장소 rename은 사용자 명시 승인 후에만 수행한다. 승인 전에는 현재 origin URL을 유지한다.
- [ ] rename이 승인되면 origin, README clone URL, badge, Actions, secrets/env, releases, webhook, fresh clone을 순서대로 확인한다.

### 자체 테스트
- [x] 현재 checkout에서 Python 전체 회귀와 Android unit/lint/variant build가 재현된다.
- [ ] QA report의 `PASS`, `NOT_EXECUTED`, `DEFERRED_BY_OWNER`, `BLOCKED`가 실제 증적과 일치한다.
- [ ] README·HANDOFF·개발 계획 사이의 package ID, 모델, 서버 주소 정책, 지원 범위가 일치한다.
- [ ] release blocker가 0이거나, 남은 blocker와 필요한 사용자 승인 항목이 명확히 목록화된다.

### 이슈 및 수정
- [ ] 문서 간 상태 불일치, 오래된 package/brand, 이전 캡처, 검증 스크립트 false positive를 발견하면 이 Phase에서 수정한다.
- [ ] 외부 저장소·계정·서명처럼 권한이 필요한 항목은 실행하지 않고 승인 대기 상태로 기록한다.

### 완료 조건
- [ ] 모든 실행 결과와 미실행 사유가 문서화됨
- [x] 현재 checkout 자동 검증 통과
- [ ] 최종 release blocker와 사용자 승인 필요 항목이 분리됨
- [ ] handoff 가능한 상태로 완료

## 2026-08-14 진행 기록

| 항목 | 결과 | 증적 또는 비고 |
|---|---|---|
| Samsung preflight | PASS | `android-app/build/device-qa/20260814_092332_R3CY40PXCAP/` |
| QA APK 설치·cold start | PASS | `com.coreline.ai.voice.qa`, Android 16, `TotalTime=527ms` |
| Samsung core connected QA | PASS | `android-app/build/device-qa/20260814_094507_R3CY40PXCAP/`, failure/error 0, runtime error 0 bytes |
| Samsung post-update core connected QA | PASS | `android-app/build/device-qa/20260814_095806_R3CY40PXCAP/`, connected tests failure 0, runtime error 0 bytes; model/fixture tests 6건은 명시적 SKIPPED |
| Room migration instrumentation | PASS | `:feature-ondevice:connectedDebugAndroidTest`, `OnDeviceDatabaseMigrationTest`, 7/7 on `SM-S931N - 16` |
| QA APK update-install | PASS | `adb install -r`, `models_before=11`, `models_after=11`, package `com.coreline.ai.voice.qa` 유지 |
| SenseVoice official Wi-Fi QA | PASS | 격리 `com.coreline.ai.voice.ondevice.test`, 다운로드·SHA-256·설치·파일 STT 성공 |
| Python 전체 회귀 | PASS | `647 passed, 14 skipped, 4 deselected` |
| Android unit/lint/build | PASS | `BUILD SUCCESSFUL`, QA/deviceTest/release assemble 포함 |
| 임시 Receiver/dashboard 계약 | PASS | `/health` 200, `/dashboard` 200, summary 401/200 확인 후 종료 |
| 임시 Receiver M4A·note E2E | PASS | `/private/tmp/airvoice-e2e.FLqJLF/`, upload 201, dashboard audio 200/byte match, summary 200, note create/read 201/200 |
| 삼성 앱 실제 Wi-Fi Receiver E2E | BLOCKED | 동일 대역 확인 후 앱 연결 확인이 `failed to connect ... 192.168.0.2:18766 ... after 20000ms`; Mac 방화벽/AP 격리 조사 필요 |
| Gemma 3 1B 실요약 | NOT_EXECUTED | gated artifact와 QA fixture가 현재 단말에 없음 |
| 30분·1시간·2시간 실음원 | NOT_EXECUTED | 실제 음원·측정 fixture 미확보 |
| Room update-install 지속성 | PARTIAL | migration 7/7 및 `adb install -r`/모델 파일 수 유지 통과; 사용자 recording/STT/summary fixture 비교는 미실행 |
| OAuth 실계정·release signing | DEFERRED_BY_OWNER | 계정·서명 credential 미제공 |
