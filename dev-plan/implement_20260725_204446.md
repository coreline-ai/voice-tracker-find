# implement_20260725_204446.md

작성 일시: `2026-07-25 20:44:46 KST`

이 문서는 연결된 실제 Android 기기에서 회귀 0 안정화 항목을 직접 검증하기 위한
테스트 케이스·실행 보조 도구 개발 계획이다. 자동화 가능한 항목은 instrumentation으로,
하드웨어·시스템 STT·화면 꺼짐 항목은 재현 가능한 수동 절차로 분리한다.

## 개발 목적

- 실제 arm64 Samsung 기기에서 Recorder, 온디바이스 STT, Moonshine, Qwen 및 모델 관리의
  완료 조건을 반복 가능한 테스트 케이스로 만든다.
- 각 실행의 기기 정보·결과·logcat·성능 지표를 남겨 PASS/FAIL 판정이 가능하게 한다.
- 테스트가 기존 사용자 녹음, 설치 모델, 앱 데이터를 임의로 삭제하지 않게 한다.

## 현재 연결 상태

- ADB 경로: `/Users/hwanchoi/Library/Android/sdk/platform-tools/adb`
- 연결 기기: `Samsung SM-S931N` / `device` 상태 / USB 연결
- 결론: **USB 디버깅 승인 상태이므로 직접 instrumentation 설치·실행과 수동 기능 테스트가 가능하다.**

## 개발 범위

- 단일 연결 기기 preflight 및 증거 수집용 실행 보조 스크립트
- RecorderService 실제 녹음 명령·PCM 종료 device instrumentation
- 기존 Android instrumentation의 안정 실행 및 결과 분류
- 시스템 Korean SpeechRecognizer, 화면 꺼짐, Qwen 취소·종료, 모델 복구의 수동 테스트 케이스
- Qwen worker PID/PSS, 녹음 파일·DB terminal 상태를 기록하는 증거 수집
- 테스트 결과 템플릿과 PASS/FAIL/차단 사유 규칙

## 제외 범위

- 기능 코드의 신규 요구사항 구현
- 실제 앱 데이터 자동 삭제, release 앱 덮어쓰기, 사용자 음성 파일의 PC 반출
- 서버 업로드·클라우드 STT/LLM 검증
- 모델 다운로드 URL·라이선스·배포 정책 변경
- 루팅, trust-all, 우회 권한, 외부 오디오 injection 장비 도입

## 참조 문서

- [회귀 0 안정화 개발 계획](implement_20260725_194227.md)
- [기기 설치 및 설정 안내](../SETUP.md)
- `android-app/app/src/androidTest/kotlin/com/thinktank/recorder/next/ondevice/`
- `android-app/feature-ondevice/src/androidTest/kotlin/com/thinktank/recorder/ondevice/`

## 공통 진행 규칙

- 대상 serial은 실행 인자로 명시하고, `adb devices`에서 `device` 상태가 1대일 때만 실행한다.
- 기본 대상은 기존 debug 앱과 분리된 `com.thinktank.recorder.next.qa` QA package다. `pm clear`, 모델 삭제, 비행기 모드 전환은 각각 명시적 사용자 확인 후에만 수행한다.
- 로컬 AI 입력·전사문·녹음 원본은 PC로 복사하지 않는다. 증거에는 시간, 길이, hash, 상태, PID/PSS만 기록한다.
- 모델 native smoke는 제품 UI 또는 승인된 로컬 import로 설치된 모델만 사용한다. 네트워크를 켜지 않는다.
- 한 케이스 실패 시 logcat, 기기 정보, 앱 상태를 수집한 뒤 다음 케이스로 넘어간다. 실패를 skip으로 바꾸지 않는다.
- 각 Phase의 자체 테스트가 완료되기 전 다음 Phase를 실행하지 않는다.

## 예상 변경 파일 / 책임

| 파일 | 책임 |
|---|---|
| `android-app/scripts/run_device_qa.sh` | ADB preflight, isolated QA package Gradle 실행, logcat·기기 정보·PSS 증거 수집. 기본 비파괴 실행 |
| `android-app/app/src/androidTest/.../recording/RecorderServiceDeviceTest.kt` | 실제 START/STOP·빠른 재시작·파일/DB terminal 상태 검증 |
| `android-app/app/src/androidTest/.../ondevice/LocalAudioRecorderSmokeTest.kt` | WAV 생성뿐 아니라 stop/release 후 파일 terminal 검증 보강 |
| `android-app/feature-ondevice/src/androidTest/.../summary/QwenInferenceProcessTest.kt` | 결과/실패 후 worker 종료 시간과 Binder death 증거 보강 |
| `dev-plan/implement_20260725_204446.md` | 수동 테스트 케이스, 실행 결과, PASS/FAIL 증거 기록 |

## 테스트 케이스 매트릭스

| ID | 구분 | 사전 조건 | 핵심 절차 | PASS 기준 | 증거 |
|---|---|---|---|---|---|
| DEV-00 | 연결 preflight | USB 디버깅 승인 | serial·ABI·SDK·배터리·여유 저장소·앱 버전 확인 | arm64, battery 50% 이상, 모델 시험 시 여유 2GB 이상 | `device.txt` |
| REC-01 | 자동 | `RECORD_AUDIO` 허용 | RecorderService START→START→STOP | runner 1개, session/chunk terminal, orphan part 0 | JUnit XML, logcat |
| REC-02 | 자동 | `RECORD_AUDIO` 허용 | PCM WAV 생성→stop→release | 16kHz mono WAV, thread/stream 종료, READY 또는 명확한 실패 | JUnit XML |
| REC-03 | 수동 | 마이크 권한·앱 화면 | 1분 녹음 중 홈→화면 끔→복귀→정지 | 녹음 복구, terminal DB/file 일치, microphone/wakelock 잔류 0 | 화면 기록, logcat |
| REC-04 | 수동 | 마이크 권한·앱 화면 | 시작/정지/즉시 시작 10회 | 서비스 중단·foreground 알림 소실·orphan part 0 | 결과표, logcat |
| STT-01 | 수동 | Korean on-device recognizer 설치 | 비행기 모드+Wi-Fi off에서 20초 한국어 발화 후 정지 | 5초 내 terminal 1회, 전사문 저장, 온라인 전환 0 | 화면 기록, logcat |
| STT-02 | 수동 | STT-01 | 인식 중 홈/뒤로가기/회전 후 복귀 | 세션 고착 0, 늦은 callback이 결과를 덮어쓰지 않음 | 결과표 |
| MOON-01 | 자동 | Moonshine 모델·승인된 Korean WAV fixture | 설치 모델 smoke 실행 | 한국어 핵심어 포함, 종료 후 native lease 0 | JUnit XML, elapsed ms |
| QWEN-01 | 자동 | Qwen 미설치 | Qwen recoverable failure process test | main process 외 worker, 5초 내 종료 | JUnit XML, PID log |
| QWEN-02 | 자동 | Qwen 모델 설치 | grounded Korean summary smoke | title/bullet/source hash 생성, Binder death 후 결과 확정 | JUnit XML, elapsed/PSS |
| QWEN-03 | 수동 | Qwen 모델 설치 | 요약 중 취소, 즉시 Moonshine 또는 모델 관리 진입 | orphan worker 0, 다음 작업 가능, transcript 보존 | PID/PSS, 화면 기록 |
| MODEL-01 | 수동 | 모델 import 파일 | 설치 중 취소→재시작→삭제 | READY/파일/WorkInfo 일치, staging/backup/partial 잔류 0 | 파일 목록, 화면 기록 |
| REG-01 | 자동+수동 | debug 앱 | existing Compose/DB migration/notes/recording 회귀 | instrumentation failure 0, local AI recording upload 0 | JUnit XML, request log |

## Phase 상태 요약

- [x] Phase 1. 실행 보조 도구·안전 장치
- [x] Phase 2. Device instrumentation 테스트 케이스 및 non-model 실제 기기 실행
- [ ] Phase 3. 수동 하드웨어·native 모델 테스트 — 모델/수동 절차 대기
- [ ] Phase 4. 증거 검토·release gate 판정 — 필수 장시간·모델 케이스 대기

## 2026-07-25 Samsung 실제 기기 실행 결과

### 실행 환경

- 실행 시각: `2026-07-25 21:09:48 KST`
- 기준 커밋: `9f328e836a0bf3823b729b021804e091894cba94` + 본 문서의 QA/회귀 보강 미커밋 변경분
- 기기: `Samsung SM-S931N` (`R3CY40PXCAP`), Android 16 / API 36, `arm64-v8a`
- 배터리: 100%, `/data` 여유 약 171 GB
- 실행 방식: 기존 debug 앱을 건드리지 않는 `com.thinktank.recorder.next.qa` 임시 QA package
- 데이터 보호: `pm clear`, 모델 삭제·다운로드, 비행기 모드/네트워크 변경, 녹음·전사문 반출을 수행하지 않았다.

### 결과표

| ID | 결과 | 실제 판정 |
|---|---|---|
| DEV-00 | PASS | serial·ABI·Android·배터리·저장소 preflight 완료, 증거 폴더 생성 |
| REC-01 | PASS | START→START→STOP 후 `STOPPED`, READY chunk 1개, 빈 `.part` 없음. 2.207초 |
| REC-02 | PASS | 실제 마이크에서 16 kHz mono WAV 생성·종료 확인. 1.152초 |
| QWEN-01 | PASS | Qwen 미설치 recoverable failure가 별도 worker에서 발생하고 5초 안에 종료됨. 0.329초 |
| 기존 feature instrumentation | PASS | DB migration·온디바이스 UI 포함 6/6 성공 |
| 기존 app instrumentation | PASS | Recorder/PCM/Compose 5개 성공, 실패 0 |
| MOON-01 | BLOCKED_MODEL | Moonshine 모델과 승인된 Korean WAV fixture가 기기에 없어 JUnit skip |
| QWEN-02 | BLOCKED_MODEL | Qwen 모델이 기기에 없어 JUnit skip 및 실제 PSS 측정 미수행 |
| REC-03/REC-04, STT-01/STT-02, QWEN-03, MODEL-01 | 미실행 | 화면 꺼짐·비행기 모드·실제 모델 설치/취소가 필요한 수동 케이스 |

- Gradle 결과: `feature-ondevice` 6 tests / failure 0, `app` 7 tests 중 5 PASS·2 SKIP·failure 0.
- 추가 단위 테스트: `app` 38/38, `feature-ondevice` 45/45 통과. Robolectric 4.13의 API 35 기본 선택 실패는 두 feature 테스트를 API 34로 고정해 해소했다.
- Android runtime error 로그: 없음.
- 테스트 종료 뒤 QA package가 기기에서 제거되어 기존 설치 앱·사용자 데이터에 영향을 남기지 않았다.
- Android 16에서 feature instrumentation APK의 낮은 target SDK 경고 대화상자가 나타나는 문제와, 기존 debug 앱의 서명 충돌을 확인했다. 전자는 instrumentation target SDK를 정렬했고, 후자는 `.qa` 분리 package로 해결해 재실행은 성공했다.

### 증거

- 기기·안전 실행 정보: `android-app/build/device-qa/20260725_210948_R3CY40PXCAP/device.txt`
- Gradle 실행 로그: `android-app/build/device-qa/20260725_210948_R3CY40PXCAP/gradle-connected.log`
- Android runtime 오류: `android-app/build/device-qa/20260725_210948_R3CY40PXCAP/android-runtime-errors.log`
- JUnit XML: `android-app/feature-ondevice/build/outputs/androidTest-results/connected/debug/` 및 `android-app/app/build/outputs/androidTest-results/connected/deviceQa/`

**현재 판정:** non-model 자동 회귀는 통과했다. 그러나 모델 성공 경로, PSS, 화면 꺼짐/장시간 녹음, 오프라인 STT 및 모델 취소·복구는 실행하지 않았으므로 production release 후보 판정은 보류한다.

## 2026-07-25 실제 APK 설치·수동 Smoke 결과

### 설치 및 실행

- 실제 설치 APK: `app-deviceQa.apk` / package `com.thinktank.recorder.next.qa` / version `1.0.0-qa`
- 설치 대상: `Samsung SM-S931N (R3CY40PXCAP)`에 streamed install 성공
- 기존 `com.thinktank.recorder.next.debug` 앱은 서명 충돌 때문에 교체하지 않았고, QA package를 별도로 유지했다.
- 실제 기기에서 온보딩 2단계 완료 후 녹음 탭과 로컬 AI 4번째 탭을 직접 실행했다.

### 실제 마이크 녹음 결과

| 항목 | 결과 |
|---|---|
| 실제 UI 시작 | PASS — 화면이 `기록 중`, 경과 시간 `00:05`, 음성 레벨과 `녹음 정지` 제어가 표시됨 |
| Foreground 서비스 | PASS — `RecorderService`가 foreground 상태로 실행됨 |
| 실제 UI 종료 | PASS — 약 20초 뒤 `기록 완료`, `녹음 시작` 재활성화 |
| 기기 내부 파일 | PASS — m4a 1개, 84,076 bytes, `.part` 0개 |
| 재시작 보존 | PASS — force-stop 후 앱 재실행에서도 `기록 완료`와 20초 세션 상태 유지 |
| 충돌 | PASS — AndroidRuntime crash 로그 없음 |

녹음 원본과 전사문은 읽거나 PC로 반출하지 않았다. QA APK와 생성된 테스트 녹음 1개는 기기에 남겨, 사용자가 후속 화면 검증을 계속할 수 있게 했다.

### 모델 다운로드 상태

- 로컬 AI 화면에는 Moonshine 약 47 MB, Qwen 약 563 MB 설치 선택지가 정상 노출된다.
- 기기 Wi-Fi는 **켜져 있으나 연결되지 않은 상태**다. 앱의 모델 설치는 Wi-Fi 전용 WorkManager 제약이므로 LTE로 우회 다운로드하지 않는다.
- 따라서 실제 모델 파일 다운로드·Moonshine 전사·Qwen 요약은 `BLOCKED_WIFI`다. Wi-Fi 연결 후 Moonshine의 라이선스 동의 화면을 사용자 확인으로 완료하고, 모델별 다운로드/무결성/실행/PSS 테스트를 이어서 수행한다.

## 2026-07-25 Wi-Fi 연결 후 Qwen 실제 모델 검증

### 다운로드·설치

- Wi-Fi: `dlink-CF8C-5GHz` 연결 확인 후 앱의 **Wi-Fi 설치**로 Qwen 모델 다운로드를 실행했다.
- 최초 실기기 실행에서 Hugging Face 응답이 10초 이상 멈추며 `SocketTimeoutException`이 발생했다. 부분 파일 약 25 MB는 유지됐다.
- `ModelDownloadWorker`의 connect timeout 30초·read/write timeout 2분을 적용하고 QA APK를 덮어 설치했다. 기존 부분 파일을 지우지 않고 이어받았다.
- 결과: `model.gguf` **563,036,064 bytes**, `installed.json` marker 및 source SHA-256 일치, WorkManager `SUCCESS`.

### 실제 기기 추론

| 항목 | 결과 |
|---|---|
| Qwen native smoke #1 | PASS — 실제 설치 모델로 제목·핵심 요약·할 일 생성, 6.072초 |
| Qwen native smoke #2 | PASS — 동일 경로 재실행, 6.156초 |
| worker 격리 | PASS — `:local_ai_qwen` 별도 worker PID 확인 |
| PSS 관측 | 62,748 KB 샘플 1회. peak PSS 장기 측정은 별도 반복 케이스로 남김 |
| worker 종료 | PASS — 두 번째 실행 완료 2초 뒤 worker PID 없음 |

테스트 전사문은 코드에 고정된 QA 문구이며, 실제 녹음·사용자 전사·요약 내용은 외부로 반출하지 않았다.

### Moonshine 상태

- Wi-Fi와 설치 화면은 준비됐다.
- Moonshine 한국어 모델 설치 버튼은 라이선스의 `읽었으며 동의` 사용자 확인 이후에만 활성화된다. 이 동의는 사용자/권한 보유자가 기기 화면에서 직접 완료해야 하므로 자동 수락하지 않았다.

## QA 관점

- 권한 거부, 저장소 부족, 마이크 선점, 앱 background/화면 꺼짐, 모델 미설치/손상, Qwen 취소 직후 재시작을 우선 공격한다.
- 기기별 시스템 STT 결과 차이는 전사 정확도 절대값이 아니라 terminal 1회·오프라인 유지·세션 상태로 판정한다.
- Qwen/Moonshine 모델이 없어서 skip된 케이스는 PASS가 아니라 `BLOCKED_MODEL`로 기록한다.
- 자동 테스트가 앱 데이터를 변경하는 경우 즉시 중지하고 debug 전용 package 또는 명시적 데이터 초기화 절차로 전환한다.

## Phase 1. 실행 보조 도구·안전 장치

### 목표

- 잘못된 기기 선택·데이터 삭제 없이 누구나 같은 기기 검증을 시작하게 한다.

### 구현 태스크

- [x] `run_device_qa.sh`를 추가하고 `--serial`, `--case`, `--destructive` 인자를 정의한다.
- [x] 지정 serial의 `device` 상태·ABI·배터리·저장소·QA package 상태를 기록하고, 비승인 기기는 실행 전에 차단한다.
- [x] 기본 실행에서 기존 debug 앱을 덮어쓰지 않고, `pm clear`, 모델 delete, 비행기 모드 변경, 음성 파일 pull을 금지한다.
- [x] `android-app/build/device-qa/<timestamp>/`에 기기 정보·Gradle 결과·필터된 logcat을 저장한다.
- [x] Qwen PID가 존재하는 동안 `dumpsys meminfo <pid>`를 1초 간격으로 수집하도록 구현한다. 실제 모델 실행 PSS는 Phase 3에서 수집한다.
- [x] 증거 파일에는 전사문·원본 음성 대신 길이·hash·상태만 남기는 필터를 적용한다.

### 자체 테스트

- [ ] 연결 기기 0대/2대/unauthorized에서 스크립트가 실행 전에 실패한다.
- [ ] `--destructive` 없이 `pm clear` 또는 모델 삭제 명령이 호출되지 않는다.
- [x] Samsung SM-S931N에서 `DEV-00` 증거 폴더가 생성된다.

### 완료 조건

- [x] 비파괴 preflight와 증거 수집이 재현 가능하다.
- [x] Phase 2 진행 가능하다.

## Phase 2. Device instrumentation 테스트 케이스

### 목표

- 사람이 매번 판정하지 않아도 되는 Recorder·PCM·Qwen worker 경로를 실제 기기에서 검증한다.

### 구현 태스크

- [x] `RecorderServiceDeviceTest`에 `REC-01` START→START→STOP 검증을 추가한다. `REC-04` 10회 수동 반복은 Phase 3에 남긴다.
- [x] 테스트가 Room 상태와 recording 디렉터리를 읽어 active session/chunk 및 `.part` 잔류를 판정하게 한다.
- [x] `LocalAudioRecorderSmokeTest`가 stop/release 뒤 WAV header·파일 길이를 검증한다. 반복 재사용성은 수동 반복으로 남긴다.
- [x] Qwen process test가 worker PID와 5초 이내 종료 시간을 구조화해 검증한다.
- [x] Moonshine/Qwen smoke는 모델 미설치 시 JUnit skip, 설치 시 elapsed time을 기록한다. 실행 결과표에서는 이를 `BLOCKED_MODEL`로 분류한다.
- [ ] Gradle filter를 DEV-00/REC-01/REC-02/QWEN-01/REG-01 묶음과 native-model 묶음으로 분리한다.

### 자체 테스트

- [x] `connectedDebugAndroidTest` 및 격리된 `connectedDeviceQaAndroidTest`의 non-model 필수 케이스가 SM-S931N에서 failure 0이다.
- [ ] 마이크 녹음 테스트가 3회 반복 모두 WAV terminal 조건을 만족한다.
- [x] Qwen 미설치 failure 경로에서 worker 종료가 5초 이내다.
- [x] 모델 미설치 skip은 위 실행 결과표에서 `BLOCKED_MODEL`로 명확히 표시한다.

### 완료 조건

- [ ] DEV-00, REC-01, REC-02, QWEN-01 자동 증거가 확보됐다. REG-01의 upload 0 및 수동 회귀 확인은 미완료다.
- [x] Phase 3 진행 가능하다.

## Phase 3. 수동 하드웨어·native 모델 테스트

### 목표

- framework callback, 화면 꺼짐, 실제 native memory처럼 instrumentation만으로 보장할 수 없는 조건을 검증한다.

### 실행 태스크

- [ ] `REC-03` 1분 background/화면 꺼짐 녹음을 수행하고 파일·DB terminal 상태를 기록한다.
- [ ] `REC-04` 빠른 시작/정지/재시작 10회를 수행한다.
- [ ] `STT-01` 비행기 모드+Wi-Fi off Korean SpeechRecognizer stop watchdog을 수행한다.
- [ ] `STT-02` 인식 중 화면 이탈·복귀와 늦은 callback 경합을 수행한다.
- [ ] Moonshine 모델과 fixture를 기기 내부 승인 경로로 준비하고 `MOON-01`을 실행한다.
- [ ] Qwen 모델을 제품 UI/import로 설치하고 `QWEN-02` success, `QWEN-03` cancel·후속 작업을 실행한다.
- [ ] Qwen 10회 실행의 peak PSS·worker death·host PSS 복귀를 기록한다.
- [ ] `MODEL-01` 설치 취소·재시작·삭제를 수행하고 staging/backup/partial 상태를 확인한다.

### 자체 테스트

- [ ] STT stop은 5초 안에 terminal 결과가 정확히 1회다.
- [ ] 20분 녹음은 별도 장시간 케이스로 화면 꺼짐 포함 성공한다.
- [ ] Qwen success/error/cancel 후 orphan worker PID가 0이다.
- [ ] Moonshine과 Qwen은 동시에 resident 상태가 아니다.
- [ ] 모델 상태와 marker/required files/WorkInfo가 일치한다.

### 완료 조건

- [ ] 하드웨어·모델 의존 케이스가 PASS 또는 BLOCKED_MODEL 근거와 함께 기록된다.
- [ ] Phase 4 진행 가능하다.

## Phase 4. 증거 검토·release gate 판정

### 목표

- 단순 실행 성공이 아닌, 안정화 계획의 수치·세션 정합성 기준을 근거로 최종 상태를 판정한다.

### 검토 태스크

- [ ] 케이스별 PASS/FAIL/BLOCKED_MODEL, 기기 OS/build, 실행 commit, 시작/종료 시각을 결과표에 확정한다.
- [ ] 실패 케이스는 logcat·DB/file 상태·PID/PSS 증거와 재현 절차를 연결한다.
- [ ] REC/STT/Qwen/model의 PASS 기준이 `implement_20260725_194227.md` 정량 기준과 일치하는지 확인한다.
- [ ] BLOCKED_MODEL을 PASS로 집계하지 않고 모델 준비 후 재실행 목록으로 분리한다.
- [ ] 모든 필수 케이스 PASS와 장시간 20분 결과가 확보됐을 때만 production release 후보로 판정한다.

### 자체 테스트

- [ ] 결과표에 필수 케이스 누락이 없다.
- [ ] 증거 폴더에 개인정보성 원본 음성·전사문이 없다.
- [ ] 자동 결과와 수동 결과의 앱 build/기기 serial이 일치한다.

### 완료 조건

- [ ] 직접 기기 QA 결과가 재현 가능하게 보관된다.
- [ ] 기존 안정화 계획의 Phase 4 체크박스를 실제 결과에 맞게 갱신할 수 있다.

## 직접 실행 시작 명령(계획 구현 후)

```bash
export ANDROID_SERIAL=<adb-device-serial>
cd android-app
JAVA_HOME='/Applications/Android Studio.app/Contents/jbr/Contents/Home' \
  ./gradlew :feature-ondevice:connectedDebugAndroidTest :app:connectedDeviceQaAndroidTest
```

현재는 기기가 이미 연결되어 있으므로, 위 계획의 Phase 1 실행 보조 도구와
Phase 2 테스트 코드를 구현한 뒤 즉시 직접 테스트를 시작할 수 있다.
